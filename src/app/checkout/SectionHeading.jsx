export const SectionHeading = ({ title, number }) => (
  <h2 className="inline-flex min-w-[9rem] md:w-[13.75rem] overflow-hidden items-center bg-primary text-white text-xs md:text-[1rem] px-4 py-2 font-bold rounded-lg mb-4">
    {title}
  </h2>
);
